[project]
name = "osm-poi-classification"
version = "0.1.0"
description = "extract and transform OpenStreetMap POI data for classification tasks"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
]
